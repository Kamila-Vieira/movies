import styled from "styled-components";

const ListContainer = styled.div`
  width: 100%;
  max-width: 900px;
  margin: 0 auto;
`;

const List = styled.ul``;

const Empty = styled.div`
  em {
    font-weight: bold;
    font-size: 18px;
  }
`;

export { ListContainer, List, Empty };
