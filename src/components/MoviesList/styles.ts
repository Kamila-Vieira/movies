import styled from "styled-components";

const ListContainer = styled.div`
  width: 100%;
  max-width: 900px;
  margin: 0 auto;
`;

const List = styled.ul``;

const Card = styled.li``;

const Poster = styled.div``;

export { ListContainer, List, Card, Poster };
